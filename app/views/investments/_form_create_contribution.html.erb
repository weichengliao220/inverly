<h1>Partial start</h1>
<%= simple_form_for [@investment, @contribution], html: { class: 'form-horizontal' } do |f| %>
  <div class="form-group">
    <%= f.input :ammount, input_html: { class: 'form-control' } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
<% end %>
<h1>Partial end</h1>
